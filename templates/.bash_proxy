# Uppercase Proxy Settings
export HTTP_PROXY="{{ ubuntu_http_proxy }}"
export HTTPS_PROXY="{{ ubuntu_https_proxy }}"
export FTP_PROXY="{{ ubuntu_http_proxy }}"
export NO_PROXY="127.0.0.1,127.0.1.1,localhost"

# Lowercase Proxy  Settings
export http_proxy="{{ ubuntu_http_proxy }}"
export https_proxy="{{ ubuntu_https_proxy }}"
export ftp_proxy="{{ ubuntu_http_proxy }}"
export no_proxy="127.0.0.1,127.0.1.1,localhost"