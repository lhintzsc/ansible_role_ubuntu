- name: Install basic packages
  apt:
    name:
      - vim
      - git
      - wget
      - curl
      - nmap
      - rsync
      - traceroute
      - ntp
      - net-tools
      - unzip
      - default-jdk
      - docker
    update_cache: false
    state: latest
  environment:
    http_proxy: "{{ ubuntu_http_proxy }}"
    https_proxy: "{{ ubuntu_https_proxy }}"
  register: result